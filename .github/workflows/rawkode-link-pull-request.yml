# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: rawkode-link-pull-request
on:
  pull_request:
    paths:
    - cue.mod/**
    - projects/rawkode.link/**
    - projects/rawkode.link/env.cue
    - projects/rawkode.link/schema/**
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
jobs:
  install:
    name: install
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv && cuenv sync -A
    - name: install
      run: cuenv task install --skip-dependencies
      working-directory: projects/rawkode.link
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
