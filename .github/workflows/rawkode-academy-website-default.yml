# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: rawkode-academy-website-default
on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/rawkode-academy-website-default.yml
    - cue.mod/**
    - projects/rawkode.academy/website/astro.config.mts
    - projects/rawkode.academy/website/bun.lock
    - projects/rawkode.academy/website/content/
    - projects/rawkode.academy/website/env.cue
    - projects/rawkode.academy/website/package.json
    - projects/rawkode.academy/website/public/
    - projects/rawkode.academy/website/schema/**
    - projects/rawkode.academy/website/src/**
  workflow_dispatch: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
  packages: write
jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: |-
        curl -sSL -o /usr/local/bin/cuenv https://github.com/cuenv/cuenv/releases/latest/download/cuenv-linux-x64 && \
        chmod +x /usr/local/bin/cuenv && \
        /usr/local/bin/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup 1Password
      run: cuenv secrets setup onepassword
      env:
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    - name: Install Bun dependencies
      run: bun install --frozen-lockfile
    - name: build
      run: cuenv task build -e production --skip-dependencies
      working-directory: projects/rawkode.academy/website
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-artifacts
        path: dist/**
        if-no-files-found: ignore
        include-hidden-files: true
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    needs:
    - build
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: |-
        curl -sSL -o /usr/local/bin/cuenv https://github.com/cuenv/cuenv/releases/latest/download/cuenv-linux-x64 && \
        chmod +x /usr/local/bin/cuenv && \
        /usr/local/bin/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup 1Password
      run: cuenv secrets setup onepassword
      env:
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    - name: Install Bun dependencies
      run: bun install --frozen-lockfile
    - name: deploy
      run: cuenv task deploy -e production --skip-dependencies
      working-directory: projects/rawkode.academy/website
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
