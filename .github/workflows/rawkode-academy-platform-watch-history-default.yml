# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: rawkode-academy-platform-watch-history-default
on:
  push:
    branches:
    - main
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
jobs:
  deploy-read:
    name: deploy.read
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv && cuenv sync -A
    - name: deploy.read
      run: cuenv task deploy.read --skip-dependencies
      working-directory: projects/rawkode.academy/platform/watch-history
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  deploy-write:
    name: deploy.write
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv && cuenv sync -A
    - name: deploy.write
      run: cuenv task deploy.write --skip-dependencies
      working-directory: projects/rawkode.academy/platform/watch-history
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  install:
    name: install
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv && cuenv sync -A
    - name: install
      run: cuenv task install --skip-dependencies
      working-directory: projects/rawkode.academy/platform/watch-history
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
