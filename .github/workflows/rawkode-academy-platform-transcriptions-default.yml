# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: rawkode-academy-platform-transcriptions-default
on:
  push:
    branches:
    - main
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    needs:
    - install
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv && cuenv sync -A
    - name: deploy
      run: cuenv task deploy -e production --skip-dependencies
      working-directory: projects/rawkode.academy/platform/transcriptions
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  install:
    name: install
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv && cuenv sync -A
    - name: install
      run: cuenv task install -e production --skip-dependencies
      working-directory: projects/rawkode.academy/platform/transcriptions
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
