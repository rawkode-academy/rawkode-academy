name: rawkode.academy/platform/email-preferences/rpc

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/platform-email-preferences-rpc.yaml"
      - "projects/rawkode.academy/generators/projen-platform-service/**"
      - "projects/rawkode.academy/platform/email-preferences/**"
  pull_request:
    paths:
      - ".github/workflows/platform-email-preferences-rpc.yaml"
      - "projects/rawkode.academy/generators/projen-platform-service/**"
      - "projects/rawkode.academy/platform/email-preferences/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install generator dependencies
        working-directory: projects/rawkode.academy/generators/projen-platform-service
        run: |
          bun install
          bun run types

      - name: Run projen for all platform services
        run: |
          for service in email-preferences email-service emoji-reactions video-likes; do
            SERVICE_DIR="projects/rawkode.academy/platform/$service"
            if [ -f "$SERVICE_DIR/.projenrc.ts" ]; then
              cd "$SERVICE_DIR"
              bun run .projenrc.ts
              cd -
            fi
          done

      - name: Run projen for all game services
        run: |
          for service in achievements leaderboard player-learned-phrases player-stats share-cards; do
            cd "projects/rawkode.academy/games/secrets-of-kubernetes/$service"
            bun run .projenrc.ts
            cd -
          done

      - name: Install service dependencies
        working-directory: projects/rawkode.academy/platform/email-preferences
        run: bun install

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          project_id: "rawkode-academy-production"
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}

      - name: Get secrets from GCP Secret Manager
        id: secrets
        uses: google-github-actions/get-secretmanager-secrets@v2
        with:
          secrets: |
            CLOUDFLARE_WORKERS_TOKEN:projects/458678766461/secrets/cloudflare-workers-token

      - name: Deploy RPC Service
        working-directory: projects/rawkode.academy/platform/email-preferences
        env:
          CLOUDFLARE_API_TOKEN: ${{ steps.secrets.outputs.CLOUDFLARE_WORKERS_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: "0aeb879de8e3cdde5fb3d413025222ce"
        run: |
          npx wrangler deploy --config ./http/wrangler.jsonc
          npx wrangler d1 migrations apply --config ./http/wrangler.jsonc --remote $(jq -r '.d1_databases[0].database_name' ./http/wrangler.jsonc) || true
