# Generated by cuenv - do not edit manually
# Regenerate with: cuenv ci --format github

name: rawkode-academy-game-sok-player-stats-default
on:
  push:
    branches:
    - main
    paths-ignore:
    - docs/**
    - examples/**
    - '*.md'
    - LICENSE
    - .vscode/**
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    needs:
    - install
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv
      run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv
    - name: deploy
      run: cuenv task deploy
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  install:
    name: install
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv
      run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv
    - name: install
      run: cuenv task install
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  run:
    name: run
    runs-on: ubuntu-latest
    needs:
    - install
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv
      run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv
    - name: run
      run: cuenv task run
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  cloudflare-types:
    name: cloudflare.types
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv
      run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv
    - name: cloudflare.types
      run: cuenv task cloudflare.types
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  collect-schemas:
    name: collect-schemas
    runs-on: ubuntu-latest
    needs:
    - install
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv
      run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv
    - name: collect-schemas
      run: cuenv task collect-schemas
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  compose:
    name: compose
    runs-on: ubuntu-latest
    needs:
    - collect-schemas
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv
      run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv
    - name: compose
      run: cuenv task compose
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
