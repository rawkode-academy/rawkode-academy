# Generated by cuenv - do not edit manually
# Regenerate with: cuenv ci --format github

name: rawkode-academy-platform-youtube-to-structured-json-pull-request
on:
  pull_request:
    paths-ignore:
    - docs/**
    - examples/**
    - '*.md'
    - LICENSE
    - .vscode/**
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
jobs:
  install:
    name: install
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv
      run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv
    - name: install
      run: cuenv task install
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  plan:
    name: plan
    runs-on: ubuntu-latest
    needs:
    - install
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv
      run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && curl -sSL https://github.com/cuenv/cuenv/releases/latest/download/cuenv-x86_64-linux.tar.gz | tar -xzf - -C /usr/local/bin && chmod +x /usr/local/bin/cuenv
    - name: plan
      run: cuenv task plan
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
