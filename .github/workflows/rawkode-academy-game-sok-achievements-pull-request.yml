# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: rawkode-academy-game-sok-achievements-pull-request
on:
  pull_request:
    paths:
    - .github/workflows/rawkode-academy-game-sok-achievements-pull-request.yml
    - cue.mod/**
    - projects/rawkode.academy/games/secrets-of-kubernetes/achievements/**
    - projects/rawkode.academy/games/secrets-of-kubernetes/achievements/env.cue
    - projects/rawkode.academy/games/secrets-of-kubernetes/achievements/schema/**
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
jobs:
  rawkode-academy-game-sok-achievements-pull-request:
    name: rawkode-academy-game-sok-achievements-pull-request
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Setup cuenv (release)
      run: |-
        curl -sSL -o /usr/local/bin/cuenv https://github.com/cuenv/cuenv/releases/latest/download/cuenv-linux-x64 && \
        chmod +x /usr/local/bin/cuenv && \
        /usr/local/bin/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: 'Run pipeline: pull-request'
      run: cuenv ci --pipeline pull-request --path projects/rawkode.academy/games/secrets-of-kubernetes/achievements
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Install Bun dependencies
      if: success()
      run: cuenv exec -- bun install --frozen-lockfile
