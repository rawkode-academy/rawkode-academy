# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: rawkode-link-default
on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/rawkode-link-default.yml
    - cue.mod/**
    - projects/rawkode.link/**
    - projects/rawkode.link/env.cue
    - projects/rawkode.link/schema/**
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: curl -sSL -o /usr/local/bin/cuenv https://github.com/cuenv/cuenv/releases/latest/download/cuenv-linux-x64 && chmod +x /usr/local/bin/cuenv && /usr/local/bin/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup 1Password
      run: cuenv secrets setup onepassword
      env:
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    - name: deploy
      run: cuenv task deploy -e production --skip-dependencies
      working-directory: projects/rawkode.link
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
