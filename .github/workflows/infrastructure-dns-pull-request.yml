# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: infrastructure-dns-pull-request
on:
  pull_request:
    paths:
    - .github/workflows/infrastructure-dns-pull-request.yml
    - cue.mod/**
    - infrastructure/dns/**
    - infrastructure/dns/env.cue
    - infrastructure/dns/schema/**
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
jobs:
  plan:
    name: plan
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Setup cuenv (release)
      run: curl -sSL -o /usr/local/bin/cuenv https://github.com/cuenv/cuenv/releases/latest/download/cuenv-linux-x64 && chmod +x /usr/local/bin/cuenv && /usr/local/bin/cuenv sync -A
    - name: plan
      run: cuenv task plan --skip-dependencies
      working-directory: infrastructure/dns
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
