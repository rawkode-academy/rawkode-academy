# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: rawkode-academy-api-default
on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/rawkode-academy-api-default.yml
    - cue.mod/**
    - projects/rawkode.academy/api/../platform/*/read-model/schema.gql
    - projects/rawkode.academy/api/../website/schema/*.gql
    - projects/rawkode.academy/api/env.cue
    - projects/rawkode.academy/api/package.json
    - projects/rawkode.academy/api/schema/**
    - projects/rawkode.academy/api/schemas/**
    - projects/rawkode.academy/api/scripts/collect-schemas.ts
    - projects/rawkode.academy/api/scripts/compose.ts
    - projects/rawkode.academy/api/src/**
    - projects/rawkode.academy/api/supergraph.graphql
    - projects/rawkode.academy/api/wrangler.jsonc
  workflow_dispatch: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
  packages: write
jobs:
  collect-schemas:
    name: collect-schemas
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: |-
        curl -sSL -o /usr/local/bin/cuenv https://github.com/cuenv/cuenv/releases/latest/download/cuenv-linux-x64 && \
        chmod +x /usr/local/bin/cuenv && \
        /usr/local/bin/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup 1Password
      run: cuenv secrets setup onepassword
      env:
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    - name: collect-schemas
      run: cuenv task collect-schemas -e production --skip-dependencies
      working-directory: projects/rawkode.academy/api
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: collect-schemas-artifacts
        path: schemas/**
        if-no-files-found: ignore
        include-hidden-files: true
  compose:
    name: compose
    runs-on: ubuntu-latest
    needs:
    - collect-schemas
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: |-
        curl -sSL -o /usr/local/bin/cuenv https://github.com/cuenv/cuenv/releases/latest/download/cuenv-linux-x64 && \
        chmod +x /usr/local/bin/cuenv && \
        /usr/local/bin/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup 1Password
      run: cuenv secrets setup onepassword
      env:
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    - name: compose
      run: cuenv task compose -e production --skip-dependencies
      working-directory: projects/rawkode.academy/api
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: compose-artifacts
        path: supergraph.graphql
        if-no-files-found: ignore
        include-hidden-files: true
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    needs:
    - compose
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup cuenv (release)
      run: |-
        curl -sSL -o /usr/local/bin/cuenv https://github.com/cuenv/cuenv/releases/latest/download/cuenv-linux-x64 && \
        chmod +x /usr/local/bin/cuenv && \
        /usr/local/bin/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup 1Password
      run: cuenv secrets setup onepassword
      env:
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
    - name: deploy
      run: cuenv task deploy -e production --skip-dependencies
      working-directory: projects/rawkode.academy/api
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
