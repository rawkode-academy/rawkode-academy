apiVersion: resources.teleport.dev/v1
kind: TeleportRoleV8
metadata:
  name: kube-admin
  namespace: teleport
spec:
  allow:
    kubernetes_groups:
      - system:masters
    kubernetes_labels:
      "*": "*"
    kubernetes_resources:
      - kind: "*"
        namespace: "*"
        name: "*"
        verbs:
          - "*"
