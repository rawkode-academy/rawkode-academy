---
import type { Bracket } from "../queries";
import BracketBoard from "../BracketBoard.vue";

interface Props {
	brackets: Bracket[];
}

const { brackets } = Astro.props;
---

<BracketBoard brackets={brackets} />
