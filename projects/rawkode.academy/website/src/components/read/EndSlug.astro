---
import { academyDocument } from "@rawkodeacademy/design-system";
const doc = academyDocument();
import MLabel from "@/components/ui/MLabel.vue";

interface Props {
	date: string;
}

const { date } = Astro.props as Props;
---

<div class={doc.end}>
	<MLabel>Published {date}</MLabel>
</div>
