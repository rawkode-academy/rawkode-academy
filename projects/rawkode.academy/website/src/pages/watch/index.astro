---
export const prerender = false;

import { getCollection } from "astro:content";
import AcademyPage from "@/components/academy/AcademyPage.vue";
import Page from "@/wrappers/page.astro";
import VideoItemListJsonLd from "@/components/html/video-itemlist-jsonld.astro";
import { getPublishedVideos } from "@/lib/content";
import { getVideoThumbnailUrl } from "@/lib/video-thumbnail";

const now = new Date();
const [publishedVideos, learningPaths] = await Promise.all([
	getPublishedVideos(),
	getCollection("learningPaths", ({ data }) => data.publishedAt <= now),
]);

const compactCopy = (value: string | undefined, maxLength = 220) => {
	const normalized = (value ?? "").replace(/\s+/g, " ").trim();
	if (!normalized)
		return "A practical session from the Rawkode Academy archive.";
	const firstSentence = normalized.match(/^.*?[.!?](?:\s|$)/)?.[0]?.trim();
	if (firstSentence && firstSentence.length <= maxLength) return firstSentence;
	if (normalized.length <= maxLength) return normalized;
	return `${normalized.slice(0, maxLength).trimEnd()}…`;
};

const formatDuration = (seconds?: number | null) => {
	if (!seconds || seconds <= 0) return "Recorded session";
	const minutes = Math.floor(seconds / 60);
	if (minutes < 60) return `${minutes} min`;
	const hours = Math.floor(minutes / 60);
	const remainder = minutes % 60;
	return remainder > 0 ? `${hours}h ${remainder}m` : `${hours}h`;
};

const videos = publishedVideos.map((video) => ({
	href: `/watch/${video.data.slug}`,
	title: video.data.title,
	description: compactCopy(video.data.subtitle || video.data.description),
	meta: [
		video.data.type === "live" ? "Live session" : "Recorded session",
		formatDuration(video.data.duration),
	],
	mediaSrc: getVideoThumbnailUrl(video.data.id),
}));

const featured = videos[0] ?? {
	href: "/watch",
	title: "The archive is the classroom.",
	description:
		"Recorded sessions and live builds for the moments when a diagram is not enough.",
	meta: ["Rawkode Academy", "Video archive"],
};

const totalHours = Math.floor(
	publishedVideos.reduce(
		(total, video) =>
			total +
			(typeof video.data.duration === "number" ? video.data.duration : 0),
		0,
	) / 3600,
);

Astro.response.headers.set(
	"Cache-Control",
	"public, max-age=300, s-maxage=3600, stale-while-revalidate=86400",
);
Astro.response.headers.set("CDN-Cache-Control", "public, max-age=3600");
Astro.response.headers.set(
	"Cache-Tag",
	"videos-page, videos-list, videos-latest",
);
Astro.response.headers.set("X-Build-Time", new Date().toISOString());

const pageTitle = "Watch Cloud Native Sessions";
const pageDescription =
	"Recorded sessions and live builds from the Rawkode Academy cloud native archive.";
---

<Page title={pageTitle} description={pageDescription}>
		<Fragment slot="extra-head">
			<link rel="alternate" type="application/rss+xml" title="Rawkode Academy - Videos (RSS)" href="/api/feeds/videos.xml" />
			<link rel="alternate" type="application/atom+xml" title="Rawkode Academy - Videos (Atom)" href="/api/feeds/videos.atom" />
			<link rel="alternate" type="application/feed+json" title="Rawkode Academy - Videos (JSON Feed)" href="/api/feeds/videos.json" />
			<VideoItemListJsonLd
				videos={publishedVideos.map((video) => video.data)}
				listUrl={new URL("/watch", Astro.site ?? Astro.url).href}
				listName="Rawkode Academy Videos"
			/>
		</Fragment>
		<AcademyPage
			client:load
			page="watch"
			featured={featured}
			latest={[]}
			videos={videos}
			learningPaths={[]}
			stats={[
				{ value: `${publishedVideos.length}`, label: "Lessons" },
				{ value: `${totalHours}+`, label: "Hours" },
				{ value: `${learningPaths.length}`, label: "Paths" },
				{ value: "2019", label: "Since" },
			]}
		/>
</Page>

<style is:global>
	html,
	body {
		margin: 0;
		padding: 0;
		min-width: 320px;
	}

	body {
		background: var(--colors-academy-canvas);
	}

	*,
	*::before,
	*::after {
		box-sizing: border-box;
	}

	a {
		color: inherit;
	}
</style>
