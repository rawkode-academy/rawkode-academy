# Rawkode Studio

Vite + Vue SPA for experimenting with browser-based live production.

## Commands

```sh
bun run dev
bun run build
```

The first screen is a StreamYard-style program canvas. HTML overlays are rendered into the canvas through an SVG `foreignObject` image and then drawn with Canvas APIs.
